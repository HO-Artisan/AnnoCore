group = ho.artisan.anno
version = 1.0.0-SNAPSHOT

# Architectury API. The Fabric and NeoForge artifacts are versioned independently upstream,
# so these are two properties rather than one.
architectury_version = 13.0.8
architectury_neoforge_version = 13.0.6
